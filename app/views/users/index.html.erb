<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li>
          <div>
            <%= user.name %>
          </div>
          <div>
            <p><%= link_to 'View Tasks', tasks_path(user) %></p>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
<% end %>